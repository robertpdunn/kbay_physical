---
title: "Kbay_physical"
author: "rpdunn"
date: "March 14, 2018"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(janitor)
library(skimr)
library(lubridate)

data<-read_csv("https://raw.githubusercontent.com/robertpdunn/kbay_physical/master/data/coral_health_allreefs.csv") #load data
str(data)

```

Tidy some data

```{r tidying data}
data_tidy<-data %>% janitor::clean_names()  #clean column names
names(data_tidy) #check it out
data_tidy<- data_tidy %>% 
  gather(key= obs_type_date,value=obs_values, dplyr::starts_with('x'))  #gather into long format, but keep 'reef', 'species' and
                                                                              #'colony' outside the gather

head(data_tidy, 10)
colnames(data_tidy)<-c("reef", "species", "colony", "date", "health_score")
head(data_tidy,10)

data_tidy$date<-gsub("x", "", data_tidy$date)
head(data_tidy, 10)

###still need to####

#add "reef_" before each observation of reef
#fix dates: flip to year_month_day

#calculate average monthly value of health score?


```



```{r }


```

